version: '3.8'

services:
  client:
    build: ./client
    ports:
      - '8080:80'
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - '8081:8081'
    env_file:
      - ./server/.env
    environment:
      - NODE_ENV=production
